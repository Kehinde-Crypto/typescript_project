"use strict";
// combination of two or more data types
let score = 33;
score = 44;
let kehinde = {
    name: "kehinde",
    id: 44
};
function getDBId(id) {
    // console.log(`getDBId is ${id}`);
    // id.toLowerCase()
    if (typeof id === 'string') {
        id.toLowerCase();
    }
    //id + 2
}
// Union in Arrays
const data = [1, 2, 3, 4];
